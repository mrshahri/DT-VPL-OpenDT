package utils;

public class MALConfigurator {
    /**
     * This class reads/writes manufacturing assembly line configurations into a resource file
     * in order to make it persistent and reloadable when the VApp restarts.
     *
     */
}
